#Deps
torchvision
pip install -r requirements.txt

# DATASET
  # URL: https://www.crcv.ucf.edu/data/UCF101.php
  # DOWNLOAD: `The UCF101 data set` & `The Train/Test Splits for Action Recognition on UCF101 data set ` you will get `UCF-101` and `ucfTrainTestlist`

# install
cd ../../resnext
    git apply ../classy_vision.patch
    python setup.py install
    pip install -e .

#run(enable ipex as default)
bash ../workload/Resnext3d/run.sh


